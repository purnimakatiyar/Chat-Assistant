# Base URL of your deployed backend (no trailing slash)
# Dev: leave empty — Vite proxy handles /api → localhost:8000
# Production (Netlify): set to your Render service URL
VITE_API_URL=https://your-service.onrender.com
